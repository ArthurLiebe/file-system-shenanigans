const { createFileWithMessage, deleteFileByName } = require('./index');

// Create a file with a message
createFileWithMessage("Hello there");

// Delete a file by its path
deleteFileByName('./yyyy-mm-dd/hh-mm-ss.txt');



// Run test.js from the terminal:
// node test.js
